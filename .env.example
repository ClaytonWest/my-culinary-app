# Frontend Environment Variables (Vite)
# These are exposed to the browser - only use VITE_ prefix for public values
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Backend Environment Variables (Convex Dashboard)
# Set these in your Convex Dashboard under Settings > Environment Variables
# DO NOT commit actual values to version control

# OpenAI API Key - Required for AI features (GPT-5.2 chat, vision, guardrails)
OPENAI_API_KEY=sk-...

# Stripe - Required for premium subscriptions (Phase 8)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PREMIUM_PRICE_ID=price_...
STRIPE_WEBHOOK_SECRET=whsec_...

# App URL - Used for Stripe redirect URLs
APP_URL=http://localhost:5173
